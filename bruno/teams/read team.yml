info:
  name: read team
  type: http

http:
  method: GET
  url: http://localhost:3000/api/teams/:id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​